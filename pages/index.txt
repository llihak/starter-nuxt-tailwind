<template>
	<div class="container mx-auto mt-10 text-xl text-center text-gray-600">
		<div class="flex flex-col items-center justify-center w-full bg-gray-100 rounded-md shadow-md">
			<div v-for="test in tests" :key="test.id" class="flex flex-col justify-start w-full p-4">
				<h1 class="py-3 text-3xl border-b-2">{{ test.greeting }}</h1>
				<p class="py-3">This is the {{ test.project }} project.</p>
				<p>{{ test.description }}</p>
			</div>
		</div>
	</div>
</template>

<script>
import getTestsQuery from '@/apollo/queries/tests/getTests';

export default {
	data() {
		return {
			tests: [],
		};
	},

	apollo: {
		tests: {
			prefetch: true,
			query: getTestsQuery,
		},
	},
};
</script>

<style></style>
